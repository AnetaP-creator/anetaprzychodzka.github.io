/* Podstawy */
:root {
  --maxw: 1200px;
}
* { box-sizing: border-box; }
html, body { margin: 0; padding: 0; }
body {
  font-family: system-ui, -apple-system, "Segoe UI", Roboto, Arial, sans-serif;
  line-height: 1.6;
  background: #0b0f1a;   /* kosmiczne tło */
  color: #f5f6fa;
}

/* Baner u góry */
header {
  margin: 0;
  padding: 0;
}
header img {
  display: block;
  width: 100%;
  height: auto;
  max-width: var(--maxw);
  margin: 0 auto;
}

/* Sekcje z obrazami */
section {
  max-width: var(--maxw);
  margin: 0 auto;
  padding: 16px;
}
section img {
  display: block;
  width: 100%;
  height: auto;
  margin: 16px 0;
  border-radius: 6px;
}

/* Stopka */
footer {
  max-width: var(--maxw);
  margin: 24px auto;
  padding: 16px;
  text-align: center;
  color: #c9d1d9;
}

/* Ukryj player audio (gra w tle) */
audio { display: none; }

/* Linki */
a { color: #58a6ff; text-decoration: none; }
a:hover { text-decoration: underline; }
