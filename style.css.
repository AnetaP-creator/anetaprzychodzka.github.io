/* ===== Wszechświat Cię Słyszy — styl ===== */
:root{
  --gold:#f7d774;
  --text:#f5f0e6;
  --btn-bg:#3a37b0;
  --btn-bg-hover:#2c2990;
  --shadow:0 12px 40px rgba(0,0,0,.45);
  --glow:rgba(247,215,116,.55);
}

*{box-sizing:border-box}
html,body{height:100%}
body{
  margin:0;
  color:var(--text);
  background:#0b0f2a;
  font-family:Inter,system-ui,-apple-system,"Segoe UI",Roboto,Arial,"Noto Sans",sans-serif;
}

/* HERO */
.hero{
  position:relative;
  min-height:100vh;
  display:grid;
  place-items:center;
  overflow:hidden;
}
.hero__bg{
  position:absolute;
  inset:0;
  background-image:url('baner_wszechswiatcieslyszy.jpg'); /* ← plik w repo */
  background-size:cover;
  background-position:center;
  filter:saturate(1.05) contrast(1.05);
  z-index:0;
}
.hero__overlay{
  position:absolute;
  inset:0;
  background:radial-gradient(ellipse at center, rgba(255,255,255,.06) 0%, rgba(10,12,32,.6) 55%, rgba(6,8,24,.9) 100%);
  z-index:1;
  pointer-events
