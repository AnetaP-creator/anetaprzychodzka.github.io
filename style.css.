/* Podstawy */
:root {
  --maxw: 1200px;
}

* { box-sizing: border-box; }

html, body { margin: 0; padding: 0; }

body {
  font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
  line-height: 1.6;
  background: #0b0f1a;   /* kosmiczne tło */
  color: #f5f6fa;
}

/* Baner u góry */
header { margin: 0; padding: 0; }

header img {
  display: block;
  width: 100%;
  height: auto;
  max-width: var(--maxw);
  margin: 0 auto;
}

/* Sekcje z obrazami */
section {
  max-width: var(--maxw);
  margin: 0 auto;
  padding: 16px;
}

section img {
  display: block;
  width: 100%;
  height: auto;
  margin: 16px 0;
  border-radius: 4px;
}

/* Stopka */
footer {
  max-width: var(--maxw);
  margin: 24px auto;
  padding: 0 16px 32px;
  text-align: center;
  font-size: .95rem;
  color: #cfd6e3;
}

/* Przycisk muzyki */
#play-btn {
  display: inline-block;
  margin: 16px;
  padding: 10px 16px;
  border: none;
  border-radius: 8px;
  background: #2d6cdf;
  color: #fff;
  font-size: 16px;
  cursor: pointer;
  box-shadow: 0 2px 8px rgba(0,0,0,.25);
}

#play-btn:hover { filter: brightness(1.08); }

/* Ukryty natywny odtwarzacz */
audio { display: none; }

/* --- GWIAZDY NA BANERZE --- */
header {
  position: relative;
  overflow: hidden;
}

header img {
  display: block;
  width: 100%;
  height: auto;
}
/* Poświata skrzydeł anioła */
header::after {
  content: "";
  position: absolute;
  top: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 120%;
  height: 100%;
  background: radial-gradient(circle at center, rgba(255, 255, 255, 0.5) 10%, transparent 60%),
              radial-gradient(circle at center, rgba(255, 215, 0, 0.3) 15%, transparent 70%);
  mix-blend-mode: screen;
  pointer-events: none;
  z-index: 1;
}
/* półprzezroczysta „siatka” gwiazdek nad obrazem */
header::before {
  content: "";
  position: absolute;
  inset: 0;
  background-image:
    radial-gradient(rgba(255,255,255,.9) 1px, transparent 1.5px),
    radial-gradient(rgba(255,255,255,.6) .8px, transparent 1.2px),
    radial-gradient(rgba(255,255,255,.8) .6px, transparent 1px);
  background-size: 120px 120px, 90px 90px, 60px 60px;
  background-position: 0 0, 30px 15px, 60px 45px;
  animation: blinkStars 4s infinite ease-in-out;
  pointer-events: none;
  z-index: 1;               /* to stawia gwiazdy nad obrazem */
}

@keyframes blinkStars {
  0%, 100% { opacity: .6; }
  50%      { opacity: 1; }
}
