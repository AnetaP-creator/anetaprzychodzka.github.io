/* === WSZECHŚWIAT CIĘ SŁYSZY === */

/* Globalne tło: gradient w odcieniach indygo, błękitu, fioletu i różu */ body { margin: 0; padding: 0; font-family: 'Arial', sans-serif; background: linear-gradient(120deg, #3c0a74, #4f3ba9, #6b60c2, #9473d7, #d490f5, #f7b4f4, #fad8f7); background-size: 400% 400%; animation: gradientAnimation 30s ease infinite; color: white; overflow-x: hidden; position: relative; }

@keyframes gradientAnimation { 0% { background-position: 0% 50%; } 50% { background-position: 100% 50%; } 100% { background-position: 0% 50%; } }

/* Gwiazdy migoczące */ .star { position: absolute; width: 2px; height: 2px; background: white; border-radius: 50%; animation: twinkle 2s infinite ease-in-out; opacity: 0.8; }

@keyframes twinkle { 0%, 100% { opacity: 0.2; } 50% { opacity: 1; } }

/* Skrzydła anioła z wyraźną poświatą */ .angel-wings { position: absolute; top: 10%; left: 50%; transform: translateX(-50%); width: 400px; height: 400px; background: radial-gradient(ellipse at center, rgba(255,255,255,0.8) 0%, rgba(255,223,145,0.3) 60%, transparent 100%); border-radius: 50%; filter: blur(40px); z-index: 0; }

/* Planety */ .planet { position: absolute; width: 30px; height: 30px; border-radius: 50%; animation: floatPlanet 20s linear infinite; opacity: 0.7; }

.planet1 { background: radial-gradient(circle, #f0c420, #ed8739); top: 20%; left: 10%; }

.planet2 { background: radial-gradient(circle, #b0e0ff, #4481c4); top: 60%; left: 80%; }

@keyframes floatPlanet { 0% { transform: translateY(0px); } 50% { transform: translateY(-20px); } 100% { transform: translateY(0px); } }

/* Nagłówek */ header img { width: 100%; max-height: 100vh; object-fit: cover; z-index: 1; }

/* Tekst */ h1, h2, p { text-align: center; z-index: 2; position: relative; color: white; text-shadow: 1px 1px 5px rgba(0, 0, 0, 0.5); }

/* Sekcje */ section { padding: 40px 20px; position: relative; z-index: 2; }

/* Przycisk kontaktowy */ button { display: block; margin: 30px auto; padding: 12px 30px; font-size: 18px; background: #ffffffcc; color: #12002f; border: none; border-radius: 10px; cursor: pointer; transition: background 0.3s ease; }

button:hover { background: #ffebcd; }

/* Stopka */ footer { text-align: center; padding: 20px; font-size: 14px; background-color: #12002f; color: #ffffffaa; }


